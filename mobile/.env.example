# Copy to .env and set your PC's LAN IP when testing on a physical phone.
# Android emulator uses 10.0.2.2 automatically if this file is absent.
# iOS simulator / web use 127.0.0.1 automatically if this file is absent.
EXPO_PUBLIC_API_URL=http://192.168.1.100:8000
